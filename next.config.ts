import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Kosong dulu -- ditambah kalau butuh (misal images.remotePatterns
  // buat nampilin foto struk di masa depan).
};

export default nextConfig;
